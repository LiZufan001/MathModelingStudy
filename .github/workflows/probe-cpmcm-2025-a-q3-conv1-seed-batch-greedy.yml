name: Probe CPMCM 2025 A Q3 Conv1 Seed Batch Greedy

on:
  workflow_dispatch:
  push:
    branches:
      - "agent/q3-pipeline-20260911"
    paths:
      - "problems/CPMCM/2025/A/experiments/benchmark_q3_saturated_seed_batch_greedy.py"
      - ".github/workflows/probe-cpmcm-2025-a-q3-conv1-seed-batch-greedy.yml"

permissions:
  contents: read

jobs:
  seed-batch-greedy:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Sparse checkout 2025 A only
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            problems/CPMCM/2025/A
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Synthetic seed batch greedy gates
        run: |
          python -m pip install --disable-pip-version-check pytest
          python -m pytest \
            problems/CPMCM/2025/A/experiments/test_q3_critical_spill_switch.py \
            problems/CPMCM/2025/A/experiments/test_q3_critical_spill_switch_batch.py \
            problems/CPMCM/2025/A/experiments/test_q3_conv_greedy_spill_switch.py \
            -q
          python -m py_compile problems/CPMCM/2025/A/experiments/benchmark_q3_saturated_seed_batch_greedy.py
      - name: Replay saturated Conv1 then seed batch greedy
        run: |
          python problems/CPMCM/2025/A/experiments/benchmark_q3_saturated_seed_batch_greedy.py \
            --data-dir problems/CPMCM/2025/A/data/csv \
            --snapshot problems/CPMCM/2025/A/experiments/snapshots/q3_conv1_saturated_offsets.json \
            --out /tmp/q3-conv1-seed-batch-greedy.json \
            --max-switches 8 \
            --batch-prefixes 2,4,8 \
            --greedy-rounds 2
      - name: Upload seed batch greedy evidence
        uses: actions/upload-artifact@v4
        with:
          name: cpmcm-2025-a-q3-conv1-seed-batch-greedy
          path: /tmp/q3-conv1-seed-batch-greedy.json
          if-no-files-found: error
