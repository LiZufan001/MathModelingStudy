name: Probe CPMCM 2025 A Q3 Conv0 Single-Switch Checkpoint

on:
  workflow_dispatch:
  push:
    branches:
      - "agent/q3-pipeline-20260911"
    paths:
      - ".github/workflows/probe-cpmcm-2025-a-q3-conv0-single-switch-checkpoint.yml"

permissions:
  actions: read
  contents: read

jobs:
  conv0-single-switch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Sparse checkout 2025 A only
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            problems/CPMCM/2025/A
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Download Conv0 single-switch round-1 checkpoint
        uses: actions/download-artifact@v4
        with:
          name: cpmcm-2025-a-q3-conv0-single-switch-checkpoint
          path: /tmp/source
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: 34748053239
      - name: Single-switch checkpoint gates
        run: |
          python -m pip install --disable-pip-version-check pytest
          python -m pytest problems/CPMCM/2025/A/experiments/test_q3_critical_spill_switch.py -q
          python -m py_compile problems/CPMCM/2025/A/experiments/probe_q3_single_switch_checkpoint.py
      - name: Probe second bounded single-switch round
        run: |
          python problems/CPMCM/2025/A/experiments/probe_q3_single_switch_checkpoint.py \
            --data-dir problems/CPMCM/2025/A/data/csv \
            --case Conv_Case0 \
            --input-dir /tmp/source/next \
            --out-dir /tmp/q3-conv0-single-switch-r2 \
            --max-switches 4 \
            --expected-official 597051
      - name: Upload round-2 checkpoint evidence
        uses: actions/upload-artifact@v4
        with:
          name: cpmcm-2025-a-q3-conv0-single-switch-checkpoint-r2
          path: /tmp/q3-conv0-single-switch-r2
          if-no-files-found: error
